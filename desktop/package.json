{
  "name": "yzw-desktop",
  "private": true,
  "version": "2.0.0",
  "description": "音转文 桌面端（Tauri 外壳）",
  "scripts": {
    "sync": "node scripts/sync-app.mjs",
    "dev": "npm run sync && tauri dev",
    "build": "npm run sync && tauri build",
    "build:mac": "npm run sync && tauri build --bundles dmg,app",
    "build:win": "npm run sync && tauri build --bundles nsis,msi",
    "build:ios": "npm run sync && tauri ios build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
